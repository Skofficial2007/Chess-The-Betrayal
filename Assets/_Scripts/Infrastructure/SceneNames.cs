namespace ChessTheBetrayal.Infrastructure
{
    /// <summary>
    /// Scene file names exactly as they appear in Build Settings. Named once here so a rename is one
    /// edit rather than a hunt through every SceneManager.LoadScene call site.
    /// </summary>
    public static class SceneNames
    {
        public const string Game = "Game Scene";
        public const string DeviceBenchmark = "DeviceBenchmark";
    }
}
